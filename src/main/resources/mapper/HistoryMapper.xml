<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.assignment.mapper.HistoryMapper">
    <insert id="insert">
        INSERT INTO history (userId, coinId, transType, transPrice, transQuantity, beforeBalance, afterBalance,
        transTime
        ) VALUES (
        #{userId}, #{coinId}, #{transType}, #{transPrice}, #{transQuantity}, #{beforeBalance}, #{afterBalance},
        #{transTime}
        )
    </insert>
</mapper>